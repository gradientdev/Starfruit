<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginVertical="10sp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/roundededgesdark"
    android:padding="@dimen/widget_margin"
    android:theme="@style/ThemeOverlay.Starfruit.AppWidgetContainer">

    <TextView
        android:id="@+id/appwidget_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:background="@drawable/roundededges"
        android:fontFamily="@font/pulpdisplay"
        android:gravity="center"
        android:padding="10sp"
        android:text="No quote delivered yet! Click to get a new quote in the app."
        android:textAlignment="center"
        android:textColor="?attr/appWidgetTextColor"
        android:textSize="20sp"
        android:textStyle="bold" />
</RelativeLayout>